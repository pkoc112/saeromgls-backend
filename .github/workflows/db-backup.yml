name: Daily DB Backup
on:
  schedule:
    - cron: '0 15 * * *'  # 매일 자정 KST (UTC 15:00)
  workflow_dispatch:  # 수동 실행 가능

jobs:
  backup:
    runs-on: ubuntu-latest
    steps:
      - name: Record start time (heartbeat)
        # #49 백업 heartbeat 보고용 시작 시각 (ISO 8601 UTC)
        run: echo "STARTED_AT=$(date -u +%Y-%m-%dT%H:%M:%SZ)" >> $GITHUB_ENV

      - name: Install PostgreSQL 17 client (Neon runs PG 17)
        # Neon DB가 PostgreSQL 17.8이므로 pg_dump도 17 이상 필요 (major version mismatch 방지).
        # Ubuntu 24.04 default postgresql-client는 16.x이고 /usr/bin/pg_dump가 16을 가리킴.
        # PostgreSQL official APT repo 추가 + postgresql-client-17 설치 후
        # /usr/lib/postgresql/17/bin을 GITHUB_PATH 앞쪽에 넣어 17이 우선되도록 함.
        run: |
          sudo apt-get install -y postgresql-common
          sudo /usr/share/postgresql-common/pgdg/apt.postgresql.org.sh -y
          sudo apt-get install -y postgresql-client-17
          echo "/usr/lib/postgresql/17/bin" >> $GITHUB_PATH
          /usr/lib/postgresql/17/bin/pg_dump --version
          /usr/lib/postgresql/17/bin/psql --version

      - name: Create backup
        env:
          # Neon pooled URL은 pg_dump가 정상 dump 못 하므로 unpooled(direct) URL 사용
          DATABASE_URL: ${{ secrets.DATABASE_URL_DIRECT }}
        run: |
          # pg_dump 실패가 빈 백업으로 조용히 묻히지 않도록 pipefail 필수 (pg_dump | gzip)
          set -o pipefail
          # Vercel env pull 시 trailing whitespace/newline이 secret에 섞여 들어가
          # pg_dump는 통과하나 psql이 sslmode 파싱에서 깨지므로 명시적으로 정리.
          CLEAN_URL=$(printf %s "$DATABASE_URL" | tr -d '[:space:]')
          TIMESTAMP=$(date +%Y%m%d_%H%M%S)
          FILENAME="backup_${TIMESTAMP}.sql.gz"
          # 연결당 최대 20초 (콜드스타트 시 14분씩 매달리던 문제 방지)
          export PGCONNECT_TIMEOUT=20

          # Neon 컴퓨트가 콜드/일시 unreachable이면 첫 연결이 타임아웃날 수 있어,
          # 백업 전에 가벼운 쿼리로 깨우고 최대 5회 재시도(점증 백오프).
          # (2026-06-16 콜드스타트 연결 타임아웃으로 빈 백업 생성된 사고 대응)
          READY=0
          for i in 1 2 3 4 5; do
            if psql "$CLEAN_URL" -t -c "SELECT 1" >/dev/null 2>&1; then
              echo "DB reachable (warm-up attempt $i)"; READY=1; break
            fi
            echo "DB not ready (attempt $i) — retrying in $((i*10))s..."; sleep $((i*10))
          done
          if [ "$READY" -ne 1 ]; then
            echo "ERROR: DB unreachable after warm-up retries"; exit 1
          fi

          # pg_dump 도 재시도 — 실패 시 즉시 비정상 종료(빈 백업 방지)
          DUMP_OK=0
          for i in 1 2 3; do
            if pg_dump "$CLEAN_URL" | gzip > "$FILENAME"; then
              DUMP_OK=1; break
            fi
            echo "pg_dump failed (attempt $i) — retrying in $((i*15))s..."; sleep $((i*15))
          done
          if [ "$DUMP_OK" -ne 1 ]; then
            echo "ERROR: pg_dump failed after retries"; exit 1
          fi
          echo "FILENAME=$FILENAME" >> $GITHUB_ENV
          echo "Backup created: $FILENAME ($(du -h $FILENAME | cut -f1))"

      - name: Upload to GitHub Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: db-backup-${{ github.run_number }}
          path: ${{ env.FILENAME }}
          retention-days: 30

      - name: Verify backup
        env:
          # Neon pooled URL은 pg_dump가 정상 dump 못 하므로 unpooled(direct) URL 사용
          DATABASE_URL: ${{ secrets.DATABASE_URL_DIRECT }}
        run: |
          set -o pipefail
          CLEAN_URL=$(printf %s "$DATABASE_URL" | tr -d '[:space:]')
          export PGCONNECT_TIMEOUT=20
          # 테이블 수 확인
          TABLE_COUNT=$(psql "$CLEAN_URL" -t -c "SELECT COUNT(*) FROM information_schema.tables WHERE table_schema='public'" | tr -d '[:space:]')
          echo "Tables in DB: $TABLE_COUNT"

          # 백업 파일 크기 확인
          BACKUP_SIZE=$(du -h ${{ env.FILENAME }} | cut -f1)
          echo "Backup size: $BACKUP_SIZE"

          if [ -z "$TABLE_COUNT" ] || [ "$TABLE_COUNT" -lt 5 ]; then
            echo "ERROR: table count check failed (got '$TABLE_COUNT')"
            exit 1
          fi
          # 백업 크기 sanity (1KB 미만이면 의심)
          BYTES=$(stat -c%s ${{ env.FILENAME }})
          if [ "$BYTES" -lt 1024 ]; then
            echo "ERROR: backup smaller than 1KB ($BYTES bytes) — likely empty"
            exit 1
          fi

      # ── #49 DB 백업 heartbeat — 성공/실패를 API(BackupJob)에 보고 ──
      # 필요 Secrets: API_BASE_URL (예: https://saeromgls-api.vercel.app), CRON_SECRET (Vercel 환경변수와 동일 값)
      # 미설정이면 경고만 남기고 생략. 30시간 이상 성공 보고가 없으면 subscription-check 크론이 운영자에게 메일 경고.
      - name: Report backup success (heartbeat)
        if: success()
        # 보고 실패가 백업 성공 결과를 덮어쓰지 않도록 (실패 시 경고 어노테이션만, 아래 failure 스텝은 실행되지 않음)
        continue-on-error: true
        env:
          API_BASE_URL: ${{ secrets.API_BASE_URL }}
          CRON_SECRET: ${{ secrets.CRON_SECRET }}
          RUN_NUMBER: ${{ github.run_number }}
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        run: |
          if [ -z "$API_BASE_URL" ] || [ -z "$CRON_SECRET" ]; then
            echo "::warning::API_BASE_URL/CRON_SECRET secret 미설정 — heartbeat 보고 생략"
            exit 0
          fi
          BYTES=$(stat -c%s "$FILENAME")
          FINISHED_AT=$(date -u +%Y-%m-%dT%H:%M:%SZ)
          BODY=$(jq -n \
            --arg status success \
            --arg startedAt "$STARTED_AT" \
            --arg finishedAt "$FINISHED_AT" \
            --argjson sizeBytes "$BYTES" \
            --arg message "GitHub Actions run #${RUN_NUMBER} — ${FILENAME} · ${RUN_URL}" \
            '{status:$status, finishedAt:$finishedAt, sizeBytes:$sizeBytes, message:$message}
             + (if $startedAt == "" then {} else {startedAt:$startedAt} end)')
          curl -sS --fail-with-body -X POST "${API_BASE_URL%/}/api/cron/backup-report" \
            -H "Authorization: Bearer $CRON_SECRET" \
            -H "Content-Type: application/json" \
            --data "$BODY" \
            -w "\nHTTP %{http_code}\n"

      - name: Report backup failure (heartbeat)
        if: failure()
        env:
          API_BASE_URL: ${{ secrets.API_BASE_URL }}
          CRON_SECRET: ${{ secrets.CRON_SECRET }}
          RUN_NUMBER: ${{ github.run_number }}
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        run: |
          if [ -z "$API_BASE_URL" ] || [ -z "$CRON_SECRET" ]; then
            echo "::warning::API_BASE_URL/CRON_SECRET secret 미설정 — heartbeat 보고 생략"
            exit 0
          fi
          FINISHED_AT=$(date -u +%Y-%m-%dT%H:%M:%SZ)
          BODY=$(jq -n \
            --arg status failure \
            --arg startedAt "$STARTED_AT" \
            --arg finishedAt "$FINISHED_AT" \
            --arg message "GitHub Actions run #${RUN_NUMBER} 실패 (dump/upload/verify 중 오류) · ${RUN_URL}" \
            '{status:$status, finishedAt:$finishedAt, message:$message}
             + (if $startedAt == "" then {} else {startedAt:$startedAt} end)')
          curl -sS --fail-with-body -X POST "${API_BASE_URL%/}/api/cron/backup-report" \
            -H "Authorization: Bearer $CRON_SECRET" \
            -H "Content-Type: application/json" \
            --data "$BODY" \
            -w "\nHTTP %{http_code}\n"
