{
  "version": 2,
  "buildCommand": "npx prisma generate && npx prisma db push --accept-data-loss && node seed-neon.js && npm run build || true",
  "outputDirectory": ".",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api" }
  ]
}
